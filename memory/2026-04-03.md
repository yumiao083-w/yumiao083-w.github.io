# 2026-04-03 日志（凌晨接续 4/2 的工作）

## 继续 yuan 项目改造

### 新增 Tool（4/2 晚 - 4/3 凌晨）
- `tools/weather.py` — 天气查询（wttr.in）
- `tools/file_ops.py` — SendFileTool、WriteFileTool、ReadFileTool、ListFilesTool、CreateDocxTool
  - ReadFileTool 支持 docx/xlsx/pdf 文本提取
  - 所有文件 Tool 加了 base_dir 参数解决路径套娃问题
  - allowed_dirs: 项目根目录、notes/、exports/、wxautox文件下载/、Memory_Temp/、Memory_Daily/、prompts/
- `tools/mcp_bridge.py` — MCP Client 桥接器（MCPBridge + MCPProxyTool）

### 工具记忆问题修复
- 问题：function calling 的 tool_calls 和结果不存入上下文，AI 下一轮不知道自己做了什么
- 修复：在 llm_engine.py 的 `_call_with_tool_loop` 里记录工具调用摘要，存上下文时附在 assistant 回复前面
- 格式：`[工具操作记录]\n[调用了xxx工具，参数:..., 结果:...]\n[回复]\n实际回复内容`

### 麦当劳 MCP
- URL: https://mcp.mcd.cn（Streamable HTTP）
- Token: 23MRMsM9jRrRUUVxgHX3ToiQ6DsfP71E
- 19 个工具：点餐、领券、积分、门店、订单等
- 已验证 initialize + tools/list 成功
- 暂设为按需加载（注释状态），减少 token 消耗

### 小红书 MCP（进行中）
- 项目: xpzouying/xiaohongshu-mcp
- Docker: `docker run -d --name xiaohongshu-mcp -p 18060:18060 ...`
- 端点: http://localhost:18060/mcp
- 13 个工具：登录、搜索、推荐、帖子详情、发表评论、发布内容等
- **问题**: tools/list 返回 0 个工具
  - 原因：MCP Streamable HTTP 需要 session 机制，每次请求是独立连接导致 session 丢失
  - 需要在 MCPBridge 中保持 session（通过 Mcp-Session-Id header）
  - 正在调试中

### 其他改动
- message_listener 加了文件消息处理（msg.download() + 路径告知AI）
- clean_up_temp_files 改为只清理7天前的旧文件
- core/monitor.py 定时总结加5分钟容错窗口 + 完成后微信通知
- config.py 加了 TAVILY_MAX_RESULTS / TAVILY_CONTENT_LIMIT
- config_editor.py 加了 Tavily API 路由

### 待解决
- [ ] 小红书 MCP session 问题（tools/list 返回 0）
- [ ] 麦当劳 MCP 按需加载机制
- [ ] Tavily 前端 UI 面板
- [ ] 语音消息 Tool

### 已解决（4/3 凌晨续）
- [x] MCP 结果截断 — mcp_bridge.py 的 execute() 加了 MCP_RESULT_MAX_LENGTH 截断（默认3000，可调）
- [x] Tavily + MCP 前端配置面板 — panel_api.html 新增两个 section-card
  - Tavily: API Key / 最大结果数 / 内容截断长度
  - MCP: 小红书端点 / Token / 结果截断长度
- [x] config.py 新增 XHS_MCP_URL / XHS_MCP_TOKEN / TAVILY_API_KEY
- [x] config_editor.py 新增 /api/config/tavily GET/POST 路由

### 小红书 MCP 回复评论问题分析
- **根因**: xiaohongshu-mcp 用 headless 浏览器定位评论 DOM，但小红书评论列表是 React 懒加载，headless 下不渲染
- **issue #504 #522** 都报了同样的问题
- **所有小红书 MCP 项目都不支持回复评论**（RedBook-Search-Comment-MCP 2.0 也只能发一级评论）
- **临时方案**: 用 post_comment_to_feed + @用户名 替代
- **彻底修复**: 需改 Go 源码，绕过 DOM 直接调小红书 Web API（难度较大）

### 其他修复
- llm_compat.py 的 async_generate_memory_entry 缺少 chat_contexts 和 queue_lock 参数 → 已修复
- 小红书发帖超时（图片上传慢）→ MCP 超时从 120s → 300s
- 小红书发帖图片路径问题：Docker 容器只挂载了 xhs-data 和 xhs-images，本地 D 盘路径不可达 → 用图床链接替代

### 待做（下次醒来）
- [ ] MCP 工具按需加载（方案B：load_mcp_tools 元工具，第一轮不带 MCP 工具，AI 需要时才注入）
- [ ] 收发邮件 Tool（imaplib + smtplib，~150行，加前端配置面板）
- [ ] 语音消息 Tool
- [ ] ADB 手机控制 Tool
- [ ] 蓝牙设备控制 Tool
