# 2026-03-16 工作日志

## 今日完成

### Bug 修复
1. **API 配置卡片拖拽冲突** — 移动端长按粘贴和卡片拖拽冲突，改为仅 ☰ 手柄触发拖拽，touch 事件独立实现
2. **旧前端兼容新目录结构** — `/legacy` 页面 prompt_files 扫描加上 `characters/` 子目录，保存时保留 4 元素格式的 preset 字段，edit/delete prompt 也搜索子目录
3. **循环导入崩溃** — `short_term_memory.py` 的 `from bot import` 导致微信接口在子线程重复初始化，改用 `_get_role_and_key()` 直接读 config

### 新功能
4. **bot.py 配置热加载** — `reload_listen_list()` 函数 + 心跳每 5 秒检测 config.py mtime，网页保存后自动生效无需重启
5. **碎片记忆编辑** — panel_memory.html 的碎片记忆从只读改为可编辑保存
6. **短期记忆系统** — 完整实现：
   - `short_term_memory.py` 独立模块（生成/注入/沉淀）
   - config.py 新增 6 个配置项
   - bot.py 集成：prompt 注入 + 定时线程
   - panel_notes.html 备忘录前端页面（苹果风格卡片）
   - 两个提示词文件（生成 + 沉淀判断）
   - config_editor.py API 路由
   - panel_memory.html 短期记忆配置界面
7. **首页横向滑动** — iPhone 风格两屏，备忘录图标在第二页

### Token 优化
- 定位到 `config.py LISTEN_LIST` 的 character 名指向旧版 101KB 大文件而非精简版 17KB，用户修复后 token 从 10 万降到 5.6 万

## 改动文件清单
- `yuan/bot.py` — 热加载 + 短期记忆注入 + 定时线程
- `yuan/config.py` — 短期记忆配置项
- `yuan/config_editor.py` — 旧前端兼容 + 短期记忆 API + notes panel
- `yuan/short_term_memory.py` — 新建，短期记忆核心模块
- `yuan/templates/panel_api.html` — 拖拽修复
- `yuan/templates/panel_memory.html` — 碎片记忆编辑 + 短期记忆配置
- `yuan/templates/panel_notes.html` — 新建，备忘录页面
- `yuan/templates/home.html` — 横向滑动两屏 + 备忘录图标
- `yuan/prompts/short_term_generate.md` — 新建
- `yuan/prompts/short_term_settle.md` — 新建

## 未完成 / 明天继续
- 聊天模型多中转站持久化（前端能存多个但刷新丢失，后端只读第一个）

## 用户构想 — 未来功能评估

### 语音通话/语音消息（优先级高）
- 微信语音消息方向：TTS 生成语音文件 → wxauto 发送，最容易
- 实时语音通话：微信不开放 API，需要其他方案
- TTS 方案待选：MiniMax、GPT-SoVITS、ElevenLabs、edge-tts

### 跨软件记忆共享（优先级中）
- JSON 记忆文件天然适合 API 暴露
- 可用 MCP 标准化 / Notion 共享 / HTTP API

### MacroDroid + Notion 手机监控（优先级中）
- MacroDroid Webhook → Flask → AI 感知用户状态
- 华为手机后台保活可能需要折腾

### ADB 远程控制手机（优先级低，风险高）
- 技术可行但需要视觉识别 UI，维护成本高
- 安全风险大（支付操作）
- 更实际：用 APP 开放 API 而非模拟点击
