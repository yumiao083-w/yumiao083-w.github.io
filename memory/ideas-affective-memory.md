# 情感记忆检索 & yuan 项目改造方案

> 2026-03-11 与用户讨论产生，持续更新

## 核心洞察

**传统 RAG 的局限**：embedding 只做数学上的"语义相似度"匹配，不理解对话语境、情感关联、个人习惯。

**用户的独立推导**：从 embedding 概念出发，自主推导出多智能体分层记忆架构 + 情感化检索方向。

---

## 架构概览

### 五板块注入系统

```
┌──────────────────────────────────────────────────────┐
│                    每次必注入                          │
│  ┌─────────────┐  ┌──────────┐  ┌─────────────────┐  │
│  │ 1.人设卡     │  │ 2.预设    │  │ 3.核心记忆       │  │
│  │ character.md │  │preset.md │  │ core_memory     │  │
│  │ ~5k token   │  │ ~3k token│  │ ~5k→1-2k token  │  │
│  └─────────────┘  └──────────┘  └─────────────────┘  │
├──────────────────────────────────────────────────────┤
│                  智能注入                              │
│  ┌──────────────────┐  ┌───────────────────────────┐ │
│  │ 4.记忆索引（常驻） │  │ 5.检索命中详情（按需）     │ │
│  │ 181条 summary    │  │ 关键词匹配 top 5 展开     │ │
│  │ ~13k token       │  │ ~1k token               │ │
│  └──────────────────┘  └───────────────────────────┘ │
└──────────────────────────────────────────────────────┘

新版总计: ~27k token（旧版: ~200k+ token，节省 85%+）
实测: Claude 4.6 Opus 125k→59k / Gemini 3 87k→41k
```

### 数据格式

```json
{
  "id": 1,
  "date": "2025-09-28",
  "event": "户外安全, 徒步, 玉珠峰, 山难",
  "summary": "她突然对山难案例感兴趣...(30-60字情感概要)",
  "content": "...(完整详细记忆，检索命中后才注入)",
  "memo": "...(碎碎念备忘录)"
}
```

### 检索管道

```
用户消息 + 最近 3 轮对话
    ↓
路径选择（config: MEMORY_RETRIEVAL_MODE）
  ├─ 'llm'  → 全量 181 条 summary 丢给便宜模型（如 Flash）精选 top 5
  │           失败时自动降级 keyword
  ├─ 'keyword' → event 关键词匹配 top 5
  └─ 'off'  → 不检索

LLM 精筛故障转移：
  中转站 A → 超时/报错 → 中转站 B → ... → 全挂 → 降级 keyword
  连续失败 3 次的站自动冷却 120s
```

### 未来（记忆 >500 条时）
```
embedding 粗筛 top 50 → LLM 精筛 top 5
（embedding 做缩小候选池，不做最终决策）
```

---

## 进度追踪

### ✅ 已完成

1. [x] 拆分 `袁朗.md` → `character.md`（234行/17KB）+ `preset.md`（120行/9.7KB）
2. [x] 提取 83 条记忆片段 → `memory_entries.json`（含 event + summary）
3. [x] 提取 98 条日记 + 碎碎念 → 合并入 `memory_entries.json`（共 181 条）
4. [x] 全部 181 条 event + summary 由贵模型生成完毕
5. [x] 日期格式统一、字段顺序统一、按日期排序
6. [x] 新建 `memory_retrieval.py`（检索模块：索引构建 + 关键词检索 + 格式化）
7. [x] 重写 `get_user_prompt()` 为五板块架构（bot.py）
8. [x] 调用点更新：消息处理前先跑检索管道
9. [x] 用户本地测试通过，token 大幅下降
10. [x] AI 主动标记机制 — `[SAVE_MEMORY]` 碎片记忆 + `[UPDATE_CORE]` 核心记忆（preset.md 第8章）
11. [x] `extract_memory_tags()` 双标签检测 + 分派路由（bot.py）
12. [x] `async_update_core_memory()` — AI 主动触发的核心记忆更新函数（关系视角 prompt）
13. [x] 禁用 500 轮固定触发，改为 AI 自主判断何时更新核心记忆
14. [x] 统一存储：去掉 Memory_Summaries 冗余，读写都走 Memory_Core/unified_memory.json
15. [x] config_editor.py 网页端适配 Memory_Core 路径和文件名格式
16. [x] 碎片/核心记忆上下文范围从固定轮数 → 动态字符数上限（8000/10000字符）
17. [x] 碎片记忆总结 prompt 改进：要求找完整事件脉络，忽略不相关闲聊
18. [x] **记忆检索管道 v2** — LLM 精筛 + 多中转站故障转移 + 关键词降级
    - `memory_llm_ranker.py`: LLM精筛模块（故障转移链、冷却机制、JSON解析兼容多格式）
    - `memory_retrieval.py`: 路由模式（llm/keyword/off），全量 summary 丢给便宜模型精选
    - `bot.py`: 传入 recent_context（最近3轮对话）给检索管道
    - `config.py`: MEMORY_RETRIEVAL_MODE/PROVIDERS/TOP_K/FALLBACK
    - 前端配置界面：模式切换、中转站增删排序、获取模型列表、超时/降级设置

### 🔲 待实施

#### 优先级 1：测试验证
- [ ] 测试 `[SAVE_MEMORY]` 标签触发 + 碎片记忆生成效果
- [ ] 测试 `[UPDATE_CORE]` 标签触发 + 核心记忆更新效果
- [ ] 测试网页端核心记忆读写（确认路径切换正常）
- [ ] 评估碎片记忆总结质量，不佳则加第二轮清洗

#### 优先级 2：核心记忆内容精简
- [ ] 定义核心记忆的作用：只记关系动态（认知/态度/智慧），剔除具体事件
- [ ] 基于人类记忆研究设计格式：情感色调 > 关系认知 > 相处模式 > 极少数印记时刻
- [ ] 现有核心记忆 ~7k字 → 精简到 800-1500 字

#### 优先级 3：前端网页更新（放在最后做）
- [ ] 整体风格：手机桌面风格，图标入口 → 点击进入各配置/管理页面
- [ ] 风格选型：iOS / Material / 自定义，用户待选定
- [ ] config_editor 适配新的文件结构（character.md / preset.md 分开编辑）
- [ ] 记忆管理界面（查看/编辑/删除 memory_entries.json 中的条目）
- [ ] 预设切换功能（多套 preset 文件热切换）
- [ ] 世界书系统（空入口先占位）
- [ ] 去掉新手引导、商业导流
- [ ] UI 美化和手机适配

#### 优先级 4：检索管道增强（远期，记忆 >500 条时）
- [ ] embedding 预筛层（Gemini text-embedding-004，预计算 summary 向量，检索时余弦相似度取 top 50）
- [ ] embedding top 50 → LLM 精筛 top 5（两级管道）
- [ ] 新增记忆自动追加 embedding 向量
- [ ] 检索日志记录（为未来分析积累数据）

#### 优先级 5：远期
- [ ] 世界书系统（角色次要设定，关键词触发注入）
- [ ] 多角色记忆隔离
- [ ] 基于检索日志微调 embedding
- [ ] 情感化记忆检索器原型

---

## 关键文件清单

| 文件 | 位置 | 说明 |
|---|---|---|
| character.md | prompts/ | 人设卡（第1-6章） |
| preset.md | prompts/ | 预设规则（第7章） |
| 袁朗.md | prompts/ | 原始完整文件（备份） |
| memory_entries.json | 项目根目录 | 181条统一记忆（核心数据） |
| memory_retrieval.py | 项目根目录 | 检索模块 |
| migrate_memory.py | 项目根目录 | 记忆片段迁移脚本 |
| migrate_diary.py | 项目根目录 | 日记迁移脚本 |
| migration_batches/ | 项目根目录 | 迁移用的批次提示词和输出 |

---

## 设计原则

- **JSON 存储，自然语言注入**：代码读 JSON，AI 读 Markdown
- **event 触发，summary 精筛**：客观关键词做冷检索，主观概要做热检索
- **索引常驻，详情按需**：AI 始终看到全局概要，需要时才展开详情
- **向后兼容**：character.md 不存在时回退到 袁朗.md
