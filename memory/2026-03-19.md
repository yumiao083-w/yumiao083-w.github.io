# 2026-03-19 工作日志

## 今日完成

### 1. config.py 并发写入修复
- 根因：前端 `saveChat()` 用 `Promise.all` 同时发两个请求写 config.py，`_write_chat_providers` 没有文件锁
- 修复：加 FileLock + tempfile 原子写入，前端改串行保存
- 涉及文件：`config_editor.py`, `templates/panel_api.html`

### 2. 实时语音通话功能（完整实现）
- `voice_call.py` — 后端核心
  - SSE + WebSocket 双模式（默认 SSE，Flask 内置服务器不支持 WS）
  - 流式 LLM → 分句切分 → MiniMax TTS → 音频推送
  - Groq Whisper (`whisper-large-v3-turbo`) 语音识别，降级中转站
  - 完整五板块 prompt（人设+预设+核心记忆+记忆索引+短期记忆）
  - 可选：世界书、检索记忆、微信上下文（config.py 开关）
  - 语气词标签 `(laughs)` `<#0.5#>` 支持（MiniMax 原生）
  - 通话记录保存到 Voice_Logs/
  - 通话记录 CRUD API（查看/编辑/删除）
  - Whisper 幻觉文本过滤
  - 浏览器伪装头（公益站兼容）
  - 详细日志（每步带时间戳）

- `templates/panel_voice.html` — 前端
  - iOS 电话风格界面（最近通话 / 拨号 / 通话中 三视图）
  - MediaRecorder 录音 + 前端 VAD 检测（不限 Chrome）
  - 音频播放队列 + AI 说话时禁止录音
  - 文字输入框（麦克风不可用时）
  - 设置面板（齿轮图标，三个开关）
  - 通话记录详情弹窗（可编辑/删除）
  - 语气词标签前端过滤（字幕只显示纯文本）
  - 通话统计（总耗时/首token/LLM时间/TTS句数）

### 3. HTTPS + ngrok
- 自签 HTTPS：可用但体验差（浏览器拦截/卡顿），改为 config 开关默认关闭
- ngrok 隧道：集成到启动流程，自动开隧道打印手机访问地址
- config.py: `ENABLE_NGROK = True`, `NGROK_AUTH_TOKEN = ...`

### 4. API 浏览器伪装头统一
- 所有 OpenAI API 调用加上 `_BROWSER_HEADERS`（伪装 Chrome UA + 清空 X-Stainless-* 指纹）
- 涉及：voice_call.py, telegram_bot.py, config_editor.py, short_term_memory.py, batch_short_term.py

### 5. Whisper 语音识别优化
- 自动语言检测（中英文+99种语言）
- prompt 提示输出标点断句
- 幻觉过滤（「请不吝点赞订阅」「谢谢大家」等）
- 前端 VAD：无人声直接丢弃不发后端

## config.py 新增配置项
```python
VOICE_SHARE_WECHAT_CONTEXT = False  # 通话时接入微信上下文
VOICE_ENABLE_WORLD_INFO = False     # 通话时启用世界书
VOICE_ENABLE_MEMORY_RETRIEVAL = True # 通话时启用检索记忆
VOICE_CALL_LOG_DIR = 'Voice_Logs'   # 通话记录目录
ENABLE_NGROK = True                 # 启用 ngrok 隧道
NGROK_AUTH_TOKEN = '...'            # ngrok token
ENABLE_HTTPS = False                # 自签 HTTPS（默认关）
```

## 改动文件清单
- `yuan/voice_call.py` — 新建
- `yuan/templates/panel_voice.html` — 新建
- `yuan/templates/home.html` — 加语音通话入口 + 图标保存修复
- `yuan/config_editor.py` — 注册路由 + 并发写入修复 + HTTPS/ngrok
- `yuan/config.py` — 新增语音通话配置
- `yuan/templates/panel_api.html` — saveChat 改串行
- `yuan/telegram_bot.py` — 浏览器伪装头
- `yuan/short_term_memory.py` — 浏览器伪装头
- `yuan/batch_short_term.py` — 浏览器伪装头
- `yuan/requirements.txt` — 加 flask-sock

## 待做：语音通话公开版

### 需求
两种模式：
1. **开袋即食模式** — 我配好所有 API + 角色，用户进来直接通话
   - 不保存任何用户数据（一次性通话）
   - 可选填昵称/话题（不存服务器）
   - 提示词只保留人设预设（去掉私设）
   - 内置 API Key，前端完全看不到

2. **自定义模式** — 用户自己配 LLM/TTS/Whisper API 和提示词
   - 配置存浏览器 localStorage（不传服务器）
   - 我们只提供框架

### 安全考虑
- ngrok 目前暴露整个网页服务器（含配置页面），有安全风险
- 公开版需要独立路由 `/voice/public`，不经过 `@login_required`
- 私人配置和公开版完全隔离
- 需要限流防刷（每 IP 每日 N 次）

### 部署方案待定
- 当前：家里电脑 + ngrok（临时方案）
- 后续：便宜 VPS 或 Vercel/Railway
