# 2026-03-11 工作日志

## 今日完成

### 记忆系统大改造（核心工作）

1. **拆分 `袁朗.md`**（101KB/761行）
   - → `character.md`（234行/17KB，人设卡第1-6章）
   - → `preset.md`（120行/9.7KB，预设第7章 + 新增第8章记忆标记协议）

2. **记忆数据迁移**
   - 83条记忆片段从 prompt 提取 → `memory_entries.json`
   - 98条日记 + 碎碎念合并 → 共 181 条统一格式
   - 全部由贵模型生成 event + summary（分批 10 条/批）
   - 日期统一、字段统一、按日期排序

3. **新建检索模块** `memory_retrieval.py`
   - `build_memory_index()` — 生成 181 条 summary 索引列表
   - `retrieve_memories()` — 关键词匹配检索 top 5
   - `format_retrieved_memories()` — 格式化命中详情

4. **重写 `get_user_prompt()`** — 五板块架构
   - 人设卡 + 预设 + 核心记忆 + 记忆索引（常驻）+ 检索详情（按需）
   - 实测 token 下降：Claude 125k→59k / Gemini3 87k→41k

5. **AI 主动标记记忆系统**
   - preset.md 加了第8章「记忆标记协议」
   - bot.py 加了 `extract_save_memory_tag()` 检测剥离
   - bot.py 加了 `async_generate_memory_entry()` 异步生成记忆条目
   - config.py 配置了记忆总结用主模型 API

### 研究工作

- 读了 O-Mem 论文（三层记忆：Persona + Working + Episodic）
- 对比了 MemGPT、SillyTavern、小红书网关方案
- 确定了混合触发方案：AI 主动标记（主力）+ 每日整理（兜底）

## 待办事项

### 记忆系统（继续）
- [ ] 测试 AI 主动标记是否正常工作
- [ ] 核心记忆精简（5k→1-2k token）
- [ ] 每日整理改为兜底角色
- [ ] 检索管道增强（embedding + AI 精筛 + 三权重排序）
- [ ] 索引分层（条目多了以后按时间分常驻/按需）

### 代码大扫除（记忆完善后再做）
- [ ] bot.py 清理新旧残留代码（6500行精简）
- [ ] 删除不需要的功能模块
- [ ] 前端大改：
  - 去掉新手引导
  - 去掉内嵌商业网站导流
  - 适配新的文件结构（character.md/preset.md 分开编辑）
  - 加记忆管理界面（查看/编辑/删除 entries）
  - UI 美化

### 前端适配（中期）
- [ ] 预设切换功能
- [ ] 记忆条目管理界面
- [ ] config_editor 适配

## 改动文件清单
- `yuan/prompts/character.md` — 新建
- `yuan/prompts/preset.md` — 新建 + 加第8章
- `yuan/bot.py` — 重写 get_user_prompt + 加标记检测 + 加异步记忆生成
- `yuan/config.py` — ASSISTANT 配置改为主模型 API
- `yuan/memory_retrieval.py` — 新建
- `yuan/memory_entries.json` — 新建（181条）
- `yuan/migrate_memory.py` — 新建（迁移工具）
- `yuan/migrate_diary.py` — 新建（迁移工具）
- `memory/ideas-affective-memory.md` — 方案文件更新
