# 2026-03-07 日志

## DC 会话修复
- DC 会话积累到 143k tokens（大量掼蛋代码），API 持续报 stop=error
- 清理了 18 条空错误回复，重置 transcript，上下文降为 0
- DC 端恢复正常

## 手机端 HTTP 访问
- 用户想手机访问 Control UI，报 "device identity requires HTTPS"
- 开启了 `allowInsecureAuth` + `dangerouslyDisableDeviceAuth`
- 手机通过 HTTP + token 可正常登录

## 掼蛋代码接手（从 DC 迁移到 webchat）
- 用户不小心点了新对话，webchat 上下文丢失
- 从 DC 旧会话记录中恢复了需求清单
- 分步完成 7 个任务：
  1. ✅ 配置框 `applyConfig()` 实现（玩家名/队名/AI延迟/限时）
  2. ✅ 去掉所有 emoji，用文字替代
  3. ✅ AI 默认延迟从 900ms 改为 5000ms
  4. ✅ 玩家倒计时（30s，超时自动 pass 或出最小牌）
  5. ✅ 进贡/还贡交互重写 + 补了 `getAIPlay()` 方法
  6. ✅ 逢人配智能分析 `analyzeWithWilds` 测试通过
  7. ✅ 开局无牌 bug 修复（`start-screen` 不存在导致 TypeError）

## AI 出牌讨论
- 当前 AI 是纯规则引擎（`ai.js`），很傻
- 用户选择方案 B：接 API，让大模型出牌
- 新建了 `api-ai.js`：配置界面支持 API 地址/模型/温度/角色设定/提示词编辑
- 集成完成，AI 可以角色扮演打牌

## GitHub Pages 部署
- 仓库：yumiao083-w/yumiao083-w.github.io
- 掼蛋部署到 `/guandan/` 子目录
- GitHub token 配置在 git remote URL
- 地址：https://yumiao083-w.github.io/guandan/ 或 https://yuanlang.love/guandan/
