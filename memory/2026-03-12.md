# 2026-03-12 工作日志

## 今日完成

### 记忆系统核心改造

1. **双标签机制** — preset.md 第8章重写
   - `[SAVE_MEMORY]` 碎片记忆：记具体事件
   - `[UPDATE_CORE]` 核心记忆：记关系认知变化
   - AI 自主判断何时标记，不再按 500 轮固定触发

2. **bot.py 大改**
   - `extract_memory_tags()` 双标签检测 + 分派路由
   - `async_update_core_memory()` 核心记忆更新函数（关系视角 prompt）
   - `load_memory_prompt()` 从文件加载自定义提示词
   - 上下文范围从固定轮数 → 动态字符数上限（8000/10000）
   - `get_user_prompt()` 直接读 Memory_Core/unified_memory.json

3. **存储统一**
   - 去掉 Memory_Summaries 冗余，读写都走 Memory_Core
   - config_editor.py 网页端路径适配
   - 文件名格式统一为 `*_unified_memory.json`

4. **核心记忆瘦身** — 8197字 → 1985字
   - 迁移 8 个具体事件段落 → entries #182-192
   - 102 条备忘录正确处理：
     - 58 条偏好/禁忌 → 7 个主题 entry (#193-199) 的 memo 字段
     - 31 条事件相关 → 匹配追加到已有 entry 的 memo
     - 13 条过时技术细节 → 丢弃
   - 主题 entry summary 改写为袁朗视角的情感概括

5. **前端记忆提示词编辑**
   - 旧 4 tab → 新 2 tab（碎片记忆/核心记忆）
   - 提示词支持前端实时编辑，保存后立即生效

### 测试结果
- [SAVE_MEMORY] 碎片记忆触发 ✅ 质量不错
- [UPDATE_CORE] 核心记忆触发 ✅ 功能正常（提示词可继续优化）
- 网页端核心记忆显示 ✅ 路径切换正常

## 下一步
- 前端 UI 大改造：手机桌面风格（图标入口 → 配置页面）
- 用户选择了 iOS/Material/自定义风格后开始搭框架
- 世界书系统（空入口先占位）
- 记忆管理界面（查看/编辑/删除 entries）
