# 2026-03-08 日志

## 掼蛋手机适配
- 用户反馈手机端太窄、按钮被压缩
- 改动：
  1. ✅ 强制横屏（竖屏弹出旋转提示）
  2. ✅ AI 牌数改为单个蓝色图标 + 大号数字（不展示牌背）
  3. ✅ 出牌位置从中央移到各玩家名字旁
  4. ✅ 全屏模式（Fullscreen API + PWA manifest）
  5. ✅ iOS 安全区适配
- 修复了 `requestFullscreen` 在 iOS Safari 不返回 Promise 导致开始游戏无反应的 bug

## 选牌/AI 优化
- 修复选牌上移被裁切（`overflow: visible` + `padding-top`）
- AI prompt 大幅精简优化
- 新增开发者模式（配置界面开关，开启后三家明牌）

## git commits:
- `942c670` 添加掼蛋游戏
- `c8cbdd0` 手机横屏适配 + AI牌数图标 + 出牌移到各玩家名字旁
- `022b5a9` 全屏模式 + PWA支持 + iOS安全区适配
- `fca9933` 修复选牌裁切 + 优化AI prompt + 开发者模式
- `8bd5847` 修复全屏API调用错误阻断游戏启动

## yuan 项目（微信机器人魔改版）
- 用户上传了 `yuan/` 目录，基于 WeChatBot_WXAUTO_SE 的大幅魔改
- bot.py 6525行，有记忆系统、提示词模板、定时任务、intiface 控制器
- 讨论了三个需求方向：
  1. Prompt/记忆重构（参考酒馆世界书） — 工程量大，暂缓
  2. 多服务商预设管理 — 先做这个 ✅
  3. 语音消息 — 取决于底层支持
- 完成了多服务商预设管理功能：
  - `api_providers.json` 存储预设
  - 后端 4 个 API 路由（增删查 + 代理获取模型列表）
  - 前端每个 API 配置区加预设选择器 + 获取模型按钮
