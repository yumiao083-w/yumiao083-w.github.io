# 2026-03-23 工作日志

## 今日完成（HF Space voice-call-test）

### 1. TTS API 付费服务（完整系统）
- `api_service.py` — 用户系统 + 余额 + 卡密 + 计费模块
- 用户注册（昵称+密码）、登录、API Key 管理
- 卡密充值系统（管理面板批量生成，用户在 `/tts-api` 兑换）
- TTS 按量计费：speech-2.8-hd ¥3.5/万字符，turbo ¥2.0/万字符
- 用户调用记录（每次扣费详情）
- 管理面板：用户列表/手动充值/卡密管理/使用记录

### 2. 通话页三模式
- **免费体验**：密钥/口令登录，全用内置 API
- **付费通话**：访客模式，自带 LLM + TTS API Key（袁朗音色按量扣费）
- **完全自带**：访客模式，自带 LLM + 自带 MiniMax TTS
- 访客模式下已隐藏"使用内置语音"选项，防白嫖

### 3. 隐私安全修复
- **URL 参数泄露修复**：用户设定（角色、话题等）改用 sessionStorage 传递，不再出现在 URL 中
- **Flask 日志脱敏**：敏感参数自动替换为 [REDACTED]
- 用户数据确认不保存在服务端（只在浏览器内存+sessionStorage）

### 4. GitHub 数据持久化
- 私有仓库：https://github.com/yumiao083-w/voice-call-data
- 启动时从 GitHub 拉数据，变更时异步推送
- HF Space 重启不丢用户/卡密/使用记录
- 需要 HF Secrets: `GITHUB_TOKEN` = `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`

### 5. 前端调试日志
- 集成在通话设置面板底部（齿轮 → 拉到最下面）
- 自动检测：浏览器类型、MediaRecorder/getUserMedia 支持、录音格式
- 全链路日志：SSE连接、STT识别、TTS合成、音频播放、错误详情
- 一键复制日志按钮，方便用户反馈

### 6. 其他
- 首页加了 TTS API 跳转链接
- OpenClaw API Key 更新为 `sk-mYQKa2IHYTcc4x5VhhIxjkqkCdGE26GkRNTui1CmayE0N94q`
- GitHub PAT 更新为 `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`

## yuan 项目
- GLM-ASR 加入 telegram_bot.py 和 voice_call.py 作为首选 STT 引擎
- config.py 新增 ZHIPU_API_KEY 配置项（需用户填入）
- 清理公开语音残留代码（-1313行）

## 待做清单
### 功能
1. ⬜ UI 风格切换（苹果 / 千禧年诺基亚风格）
2. ⬜ 通话小结（挂断后 AI 生成袁朗口吻总结）
3. ⬜ 通用版框架（去掉角色限制，面向大众）
4. ⬜ PWA 支持（添加到主屏幕像 App）

### 付费/运营
5. ⬜ 卡密销售渠道确定（讨论中的方案）：
   - **闲鱼卖卡密**（最简单，0成本，先验证需求）
   - **第三方发卡平台**（自动发货，不暴露个人信息，抽1-3%）
   - **独角数卡自部署**（需额外服务器，但完全自控）
   核心诉求：不暴露个人收款信息，不在小红书直接转账
6. ⬜ yuan 项目 config.py 填入 ZHIPU_API_KEY

### 基础设施
7. ⬜ 云服务器（阿里云/腾讯云轻量 ¥38/年有特价）
   - 解决 HF 免费 Space 休眠问题
   - 目前用 GitHub 持久化暂时解决了数据丢失

## HF Space 当前所有 Secrets
| 变量名 | 用途 |
|--------|------|
| ADMIN_PASSWORD | 管理面板密码 |
| REDEEM_CODE | 初始口令 |
| REDEEM_DAILY_LIMIT | 口令每日限额 |
| PUBLIC_KEYS | 初始密钥 |
| MINIMAX_API_KEY | TTS |
| MINIMAX_GROUP_ID | TTS |
| MINIMAX_VOICE_ID | TTS 音色 |
| MINIMAX_TTS_MODEL | TTS 模型 |
| ZHIPU_API_KEY | GLM-ASR |
| GROQ_API_KEY | Whisper 降级 |
| LLM_PROVIDER*_URL/KEY/KEYS | LLM 中转站 |
| LLM_MODEL | 默认 LLM 模型 |
| GITHUB_TOKEN | 数据持久化 |
