# 2026-03-17 第二轮工作 - API多中转站 + TG Bot 修复

## 完成的工作

### 1. 聊天模型多中转站故障转移（完整实现）
- `config.py`: 新增 `CHAT_API_PROVIDERS` 列表
- `bot.py`: `call_chat_api_with_retry` 重写，按 providers 顺序尝试，失败自动切换
- `config_editor.py`: 新增 `/api/chat_providers` GET/POST API，保存时同步旧字段兼容
- `panel_api.html`: 
  - 前端 init() 从后端 API 加载（刷新不丢失）
  - saveChat() 调后端 API 保存
  - 卡片改折叠式条目：收起只显示序号+名称+模型，点击展开编辑

### 2. TG Bot 修复（大量问题）
- **崩溃修复**: max_tokens 参数名错(MAX_TOKENS→MAX_TOKEN)、核心记忆文件名错(_core_memory→_unified_memory)
- **prompt 六板块对标 bot.py**: 人设卡(characters/)、预设(presets/preset.md)、核心记忆(unified_memory)、记忆索引(memory_entries.json)、短期记忆
- **多中转站**: TG 端也用 CHAT_API_PROVIDERS + 流式传输
- **代理自动检测**: 模块加载时探测本地代理端口(7897/7890等)，设环境变量让 httpx 走代理
- **自动重连**: 最多重试5次 + input() 防黑框闪退
- **[TG]标签去掉**: AI 会模仿导致双标签，改为 system prompt 告知平台
- **反斜杠分消息**: 和微信端 split_message_with_context 一致
- **打字延迟**: 对标微信端公式 max(1.0, 字数×速度+random)，config 共用
- **消息队列**: QUEUE_WAITING_TIME 秒等待合并多条消息
- **config 热加载**: 检测 mtime 变化自动 reload，前端改配置不用重启

### 3. TG Bot Token 更新
- 旧号被封（虚拟号风控）
- 新 Token: 8411509133:AAH1zsVwHwLkzQLYvON0AbGAoGeFg3tI56M
- Bot 名不变: @yuanlang_love_bot

## 下一步：语音克隆
- 用户要做 TG 端语音克隆功能
- 之前调研结论：MiniMax TTS API 推荐（用户已有账号）
- 当前 TTS: edge-tts (zh-CN-XiaoxiaoNeural)
